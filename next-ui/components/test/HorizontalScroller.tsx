import { Preview } from '@/lib/context/PreviewContext'
import React from 'react'

const HorizontalScroller = ({trees}:{trees:Preview[]}) => {
  return (
    <div>HorizontalScroller</div>
  )
}

export default HorizontalScroller